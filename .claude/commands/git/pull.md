Pull the latest changes from GitHub (https://github.com/ak12459632/Auto-Check-out, branch: main) into the local Auto-Check-out-main directory.

Steps:
1. Run `git status` first — if there are uncommitted local changes, warn the user and ask whether to proceed (a pull may cause conflicts).
2. Run `git pull origin main` to fetch and merge the latest remote changes.
3. Report what files were updated (or "Already up to date." if nothing changed).

Working directory: d:/Ray_CC_project/Auto Check out
Remote: https://github.com/ak12459632/Auto-Check-out
Branch: main
