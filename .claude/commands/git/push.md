Push all local changes in the Auto-Check-out-main project to GitHub (https://github.com/ak12459632/Auto-Check-out, branch: main).

Steps:
1. Run `git status` to show what has changed.
2. If there are no changes to commit, report that and stop.
3. Stage all changes: `git add -A`
4. Ask the user for a commit message. If they don't provide one, auto-generate a concise message based on the changed files (e.g. "Update auto_check_out_v1p2.py").
5. Commit with the message, co-authored by Claude.
6. Push to origin main: `git push origin main`
7. Report success with the commit hash and a link to the repo.

Working directory: d:/Ray_CC_project/Auto Check out
Remote: https://github.com/ak12459632/Auto-Check-out
Branch: main
